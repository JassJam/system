# Example secrets file
# 1. Copy this to hosts/jass/secrets/secrets.yaml
# 2. Add your actual secrets
# 3. Encrypt with: nix-shell -p sops --run "sops -e -i hosts/jass/secrets/secrets.yaml"
# Note: This file is gitignored, so each user must create their own

ssh:
  music_server:
    hostname: your-server-ip-here
    user: your-username-here

github:
  email: your-email@example.com
  username: YourGithubUsername
